# Settings

User-facing configuration screen.

## To cover
- Language selector (EN, FR, DE, HR, ES)
- Privacy toggles (location, calls, messages, phone usage, internet tracking, notifications)
- `+1` unit definition (what counts as one drink)
- Graceful fallbacks when a toggle is off
