import SwiftUI

struct NotificationSettingsView: View {
    var body: some View {
        // TODO: global toggle, per-friend "starts drinking" toggle, nearby friend toggle
        EmptyView()
    }
}
