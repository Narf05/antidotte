import SwiftUI

struct PhotoLoggingSettingsView: View {
    var body: some View {
        // TODO: photo detection on/off, save photos toggle, default flow (quick/photo/ask)
        EmptyView()
    }
}
