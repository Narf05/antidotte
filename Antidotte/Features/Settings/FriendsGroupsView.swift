import SwiftUI

struct FriendsGroupsView: View {
    var body: some View {
        // TODO: add friends, invite code/link, groups management, per-friend visibility, blocked users
        EmptyView()
    }
}
