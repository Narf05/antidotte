import SwiftUI

struct ProfileView: View {
    @StateObject private var viewModel = ProfileViewModel()

    var body: some View {
        // TODO: identity, night status, drunk score summary, solo alcotest shortcut,
        //       event/theme summary, location/join controls, stats shortcuts, settings link
        EmptyView()
    }
}
