import SwiftUI

struct DrunknessVisibilityPicker: View {
    @Binding var selection: String

    var body: some View {
        // Options: category only / percentage only / both / hidden
        // TODO: picker with live preview of what friends will see
        EmptyView()
    }
}
