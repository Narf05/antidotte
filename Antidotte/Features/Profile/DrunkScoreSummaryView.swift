import SwiftUI

struct DrunkScoreSummaryView: View {
    var body: some View {
        // TODO: current category, confidence, signals used, last updated, last alcotest round
        EmptyView()
    }
}
