import SwiftUI

struct AccountSetupView: View {
    let next: () -> Void

    var body: some View {
        // TODO: username, display name, optional profile photo, language picker
        EmptyView()
    }
}
