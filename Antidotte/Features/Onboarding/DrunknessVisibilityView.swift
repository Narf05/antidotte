import SwiftUI

struct DrunknessVisibilityView: View {
    let next: () -> Void

    var body: some View {
        // TODO: category only / percentage only / both / hidden picker
        EmptyView()
    }
}
