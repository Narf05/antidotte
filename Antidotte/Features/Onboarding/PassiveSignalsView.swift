import SwiftUI

struct PassiveSignalsView: View {
    let next: () -> Void

    var body: some View {
        // TODO: opt-in for motion tracking, phone usage tracking, voice analysis (all off by default)
        EmptyView()
    }
}
