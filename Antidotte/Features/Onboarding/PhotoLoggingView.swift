import SwiftUI

struct PhotoLoggingView: View {
    let next: () -> Void

    var body: some View {
        // TODO: opt-in for photo-assisted drink detection, save photos toggle
        EmptyView()
    }
}
