import SwiftUI

struct PrivacyOverviewView: View {
    let next: () -> Void

    var body: some View {
        // TODO: explain each privacy toggle separately, mention panic privacy
        EmptyView()
    }
}
