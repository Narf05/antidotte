import SwiftUI
import CoreLocation

struct LocationPermissionView: View {
    let next: () -> Void

    var body: some View {
        // TODO: explain live map, request iOS location permission, offer skip
        EmptyView()
    }
}
