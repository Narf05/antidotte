import SwiftUI

struct FriendsSetupView: View {
    let next: () -> Void

    var body: some View {
        // TODO: search by username, invite via code/link (generated on demand)
        EmptyView()
    }
}
