import SwiftUI

struct CalibrationView: View {
    let next: () -> Void

    var body: some View {
        // TODO: body weight (required), optional height, sports, tolerance rating
        // Calibration data is private — explain this to the user
        EmptyView()
    }
}
