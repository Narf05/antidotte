import SwiftUI

struct StatsView: View {
    @StateObject private var viewModel = StatsViewModel()

    var body: some View {
        // TODO: score chart, time range selector, key stats, friend ranking, history feed
        EmptyView()
    }
}
