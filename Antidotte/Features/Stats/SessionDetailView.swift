import SwiftUI

struct SessionDetailView: View {
    let session: NightOutSession

    var body: some View {
        // TODO: session score chart, timeline (drinks, tests, venues), participants, spend
        EmptyView()
    }
}
