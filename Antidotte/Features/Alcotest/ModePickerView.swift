import SwiftUI

struct ModePickerView: View {
    @ObservedObject var viewModel: AlcotestViewModel

    var body: some View {
        // TODO: SINGLE PLAYER / MULTIPLAYER choice
        EmptyView()
    }
}
