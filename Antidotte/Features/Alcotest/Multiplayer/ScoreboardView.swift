import SwiftUI

struct ScoreboardView: View {
    let results: [ActiveTestResult]

    var body: some View {
        // TODO: group scoreboard after everyone finishes, playful not punitive
        EmptyView()
    }
}
