import SwiftUI

struct RoundPickerView: View {
    @ObservedObject var viewModel: AlcotestViewModel

    var body: some View {
        // TODO: QUICK ROUND / FULL ROUND / CUSTOM ROUND
        EmptyView()
    }
}
