import SwiftUI

struct RoundRunnerView: View {
    let games: [GameType]
    @ObservedObject var viewModel: AlcotestViewModel

    var body: some View {
        // TODO: step through games in sequence, collect results, show progress
        EmptyView()
    }
}
