import SwiftUI
import CoreMotion

struct HoldStillGame: View {
    let onComplete: (ActiveTestResult) -> Void

    var body: some View {
        // TODO: hold phone still, accelerometer measures shake and drift vs baseline
        EmptyView()
    }
}
