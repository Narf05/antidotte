import SwiftUI

struct VibeCheckGame: View {
    let onComplete: (ActiveTestResult) -> Void

    var body: some View {
        // TODO: self-report questions about current state — feeds as a signal not the score
        EmptyView()
    }
}
