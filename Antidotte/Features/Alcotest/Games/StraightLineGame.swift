import SwiftUI

struct StraightLineGame: View {
    let onComplete: (ActiveTestResult) -> Void

    var body: some View {
        // TODO: user traces a line or path, measures coordination and steadiness
        EmptyView()
    }
}
