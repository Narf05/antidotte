import SwiftUI

struct ReadItRightGame: View {
    let onComplete: (ActiveTestResult) -> Void

    var body: some View {
        // TODO: user reads a phrase aloud — voice analysis (only if voice_analysis_enabled = true)
        // Raw audio must be processed on-device or deleted immediately after analysis
        EmptyView()
    }
}
