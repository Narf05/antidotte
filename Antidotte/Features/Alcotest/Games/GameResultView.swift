import SwiftUI

struct GameResultView: View {
    let result: ActiveTestResult

    var body: some View {
        // TODO: show result for one game, transition to next game or round summary
        EmptyView()
    }
}
