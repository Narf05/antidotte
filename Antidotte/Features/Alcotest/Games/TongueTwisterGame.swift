import SwiftUI

struct TongueTwisterGame: View {
    let onComplete: (ActiveTestResult) -> Void

    var body: some View {
        // TODO: harder voice clarity game — tongue twister phrase, slurring detection
        // Only available if voice_analysis_enabled = true
        EmptyView()
    }
}
