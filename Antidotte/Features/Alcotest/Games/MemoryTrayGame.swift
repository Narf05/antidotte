import SwiftUI

struct MemoryTrayGame: View {
    let onComplete: (ActiveTestResult) -> Void

    var body: some View {
        // TODO: show items briefly, user recalls them — measures short-term memory
        EmptyView()
    }
}
