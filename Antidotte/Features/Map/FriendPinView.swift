import SwiftUI

struct FriendPinView: View {
    let presence: FriendPresence

    var body: some View {
        // TODO: position on map, show initials + tipsiness badge + join status dot
        EmptyView()
    }
}
