import SwiftUI

struct GroupClusterView: View {
    let presences: [FriendPresence]

    var body: some View {
        // TODO: show group bubble on map, expand to list of members
        EmptyView()
    }
}
