import SwiftUI

struct LocationPrecisionButton: View {
    @Binding var precision: LocationPrecision

    var body: some View {
        // TODO: show current precision mode, tap to show picker (exact / approx 150m / off)
        EmptyView()
    }
}
