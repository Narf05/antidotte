import SwiftUI

struct FriendProfilePopout: View {
    let presence: FriendPresence

    var body: some View {
        // TODO: name, photo, tipsiness badge, join status, event/theme,
        //       location precision label, actions (join, message, view group)
        EmptyView()
    }
}
