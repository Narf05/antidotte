import SwiftUI

struct ThemeBottomSheet: View {
    @StateObject private var viewModel = ThemeViewModel()

    var body: some View {
        // TODO: session name, theme preset, drink defaults, mood/role, calendar, visibility
        EmptyView()
    }
}
